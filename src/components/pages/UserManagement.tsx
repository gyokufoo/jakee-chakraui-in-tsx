import { VFC, memo } from "react";
import { Outlet } from "react-router-dom";

export const UserManagement: VFC = memo(() => {
  return (
    <>
      <p>This is UserManagement Page</p>
    </>
  );
});
